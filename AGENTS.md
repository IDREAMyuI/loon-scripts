# 仓库维护约束

本文件是本仓库面向 Codex 及其他维护者的约束来源。修改任何配置、规则、插件、脚本或说明前，应先阅读本文件和 `SECURITY.md`。

## 项目目标

维护一套可公开托管的 Loon 配置、远程规则、插件和脚本。敏感订阅及证书材料只保存在手机本机，公开仓库只维护非敏感内容。

## 不可破坏的设计

1. ChatGPT / OpenAI 只能使用台湾节点，不得回退到香港、日本、手动选择或通用代理组；台湾节点全部不可用时允许连接失败。
2. 日常自动代理使用 `fallback`，固定顺序为台湾、香港、日本。
3. 提供包含完整 Sub-Store 订阅的手动选择组。
4. 普通代理流量通过顶层选择组在“日常代理”和“手动选择”之间切换，默认项必须为日常代理。
5. 自动策略使用 `fallback` 而不是 `url-test`，避免因延迟波动频繁更换出口 IP。
6. 中国大陆和局域网流量直连。
7. OpenAI 第一方核心域名、`GEOIP,CN` 和 `FINAL` 保留在主配置，作为远程规则下载失败时的最小安全兜底。
8. 完整 OpenAI 与直连规则由本仓库的 Raw 地址远程加载，策略映射保留在主配置。
9. MITM 总开关保持开启；证书只在手机本机生成、安装和信任；不得长期使用 `hostname = *`。

## 本机与公开仓库边界

- Sub-Store 中建立名称完全为 `LoonNodes` 的组合订阅或集合。
- 机场原始订阅、节点凭证及 Sub-Store 私有输出只保存在手机本机。
- 公开配置通过 `https://sub.store/download/collection/LoonNodes` 访问本机集合。
- Sub-Store 官方插件保持独立，不复制进自制插件。
- 一个 App 使用一个独立插件；同一 App 内始终一起启用的功能可以合并。

## 当前功能

- 主配置：`config/Loon_Public_v2.conf`
- OpenAI 规则：`rules/openai.list`
- 直连规则：`rules/direct.list`
- 掌阅插件：`plugins/ireader_splash_ad.lpx`
- 掌阅脚本：`scripts/ireader_disable_screen.js`
- 两步路插件：`plugins/liangbulu_ads.lpx`
- 两步路脚本：`scripts/liangbulu_disable_ads.js`

掌阅插件处理 `https://saad.ms.zhangyue.net/ad/cfg` 的响应，只修改 `slotId === "SCREEN"` 的规则并关闭开屏广告相关开关。

两步路插件处理 `helper.2bulu.com` 的广告配置和开屏内容响应，关闭开屏与插屏广告配置并清空开屏内容；不拦截第三方广告素材域名。

## 敏感输入处理

用户提供的抓包、HAR、日志、截图、二维码、压缩包、数据库、证书和配置备份默认按敏感材料处理，即使用户未特别标注。

1. 接收材料仅代表获准在当前任务中分析，不代表获准提交、上传、分享或长期保存。
2. 原始材料必须停留在仓库目录之外；不得复制到工作树、分支、提交、Issue、Pull Request、代码注释或测试夹具。
3. 优先使用本地工具检查；未经用户明确同意，不把原始内容发送到第三方网站、在线解码器、粘贴服务或其他外部服务。
4. 输出和过程日志只描述结构与结论，不复述完整 URL、请求头、Cookie、Token、账号、设备标识、节点 URI 或证书内容。
5. 必须展示示例时，使用人工构造值；需要保留格式时只显示最少字符，并保证不能据此恢复原值。
6. 压缩包与嵌套文件递归检查；截图检查可见文字、二维码和元数据；编码、Base64 或 JWT 形式不视为已脱敏。
7. 原始材料不由维护流程自动删除、移动或修改；如需清理，先确认精确路径并取得用户明确授权。
8. 任何准备写入仓库的派生内容都必须再次按 `SECURITY.md` 检查，不能假设先前已脱敏。

## 去广告插件安全约束

1. 先确认目标接口和最小必要 hostname，禁止为方便抓包长期保留 `*` 或无关域名。
2. 脚本只修改实现目标所需字段，不采集、上传或持久化请求与响应内容。
3. 日志不得输出完整请求头、响应正文、用户标识或广告请求中的追踪参数。
4. 解析失败或响应结构变化时保持原响应，不生成包含调试数据的新响应。
5. 仓库中的测试样本优先人工构造；不得直接使用真实 HAR、真实账号响应或未经验证的第三方样本。
6. 一个 App 一个插件；新增 hostname、脚本权限或数据处理范围必须在计划中明确列出。

## 修改流程

1. 修改前先列出计划和预计涉及的文件。
2. 从最新 `main` 创建新分支，不直接修改 `main`。
3. 优先创建 Draft Pull Request。
4. 每次修改后检查整个仓库：
   - 是否出现订阅 Token、UUID、密码、Cookie、Authorization、`ca-p12`、`ca-passphrase` 或私钥；
   - ChatGPT 策略是否仍只引用台湾节点；
   - 日常自动代理是否仍为台湾、香港、日本的顺序；
   - 手动选择是否仍包含完整 Sub-Store 节点；
   - 顶层代理模式是否仍默认选择日常代理；
   - 远程规则失败时，OpenAI 第一方流量是否仍只能走台湾；
   - 是否扩大 MITM hostname；
   - 配置、规则、插件和脚本的 Raw 地址是否可读；
   - 是否误提交抓包、HAR、日志、截图、二维码、证书、压缩包或真实测试响应；
   - 代码、注释、日志和测试数据中是否残留可识别个人或设备的信息。

## 已确认的协议事实

- 节点协议包括 Shadowsocks、VLESS Reality Vision 和 Hysteria2。
- Sub-Store 的 Loon 预览中 VLESS 输出包含 `flow=xtls-rprx-vision`。
- 地区筛选需要兼容节点名称中的 `JP`、`TW`、`HK` 和 `HKT` 等独立代码。
