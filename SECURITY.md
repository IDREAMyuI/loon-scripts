# 安全与隐私边界

本仓库是公开仓库。所有准备提交的内容都应按“任何人可以永久复制和检索”来判断风险。

## 默认敏感材料

以下材料无论是否看起来可读，均默认视为敏感：

- HAR、PCAP/PCAPNG、代理会话、网络日志和应用调试日志；
- 截图、录屏、二维码、压缩包、数据库和应用备份；
- Loon、Sub-Store、机场或节点配置；
- `.p12`、`.pfx`、私钥、证书密码和 MITM CA 材料；
- 包含真实请求或响应的 JSON、文本和测试夹具。

原始材料只用于本地分析，不得进入仓库、Git 历史、Issue、Pull Request、Release、代码注释或公开聊天。不要把敏感值提交后再依赖删除或覆盖来补救。

## 向 Codex 提供材料

- 只提供与当前目标有关的文件，避免整机备份或无关 App 的抓包。
- 上传前尽量先删除已知的账号、Cookie、Authorization、订阅链接、节点凭证和证书私钥。
- 无法安全预先脱敏时，应明确说明材料可能含敏感信息，并说明需要分析的 App、接口或现象。
- 附件仅授权用于当前任务的分析；除非用户另行明确授权，不得据此创建公开文件、提交、分支、Pull Request 或外部分享。
- Codex 的回复、计划、日志和生成代码不得包含可用凭证；只返回脱敏后的发现和最小必要字段结构。

## 严禁公开

- 机场原始订阅 URL、订阅标识及 Sub-Store 私有输出链接；
- Shadowsocks、VLESS、Hysteria2 等节点 URI、服务器凭证、UUID、密码和私钥；
- Cookie、`Set-Cookie`、Authorization、Proxy-Authorization、API Key 和会话令牌；
- access token、refresh token、JWT、签名、临时凭证及带鉴权参数的 URL；
- `ca-p12`、`ca-passphrase`、客户端证书私钥和证书备份密码；
- 账号、姓名、手机号、邮箱、地址和付费或订单信息；
- IDFA、IDFV、IMEI、序列号、推送 Token、设备指纹、精确位置和可关联个人的 IP；
- 抓包中的消息正文、上传内容、搜索记录或其他私人内容。

## 收到抓包后的处理流程

1. 原始文件保留在仓库目录之外，不复制进待提交目录。
2. 先确认目标 App、目标功能和必要接口，只分析与目标相关的最小范围。
3. HAR 至少检查：
   - 请求和响应 headers；
   - cookies 与 `Set-Cookie`；
   - query string；
   - POST/PUT/PATCH 请求正文；
   - response content；
   - WebSocket 消息和重定向 URL。
4. 截图与录屏检查可见账号、通知、状态栏信息、二维码和可恢复链接；必要时同时检查元数据。
5. 压缩包、备份和数据库按嵌套内容递归检查，不能只看文件名。
6. Base64、URL 编码、JWT、二维码或哈希包装不等于脱敏；能够还原或继续使用的值仍是敏感信息。
7. 分析结论只记录接口结构、字段名称和必要行为，不记录真实字段值。
8. 优先创建人工构造的最小样本；确需引用真实结构时，删除无关字段并用不可用的占位值替换。

## 插件与脚本发布要求

- MITM hostname 只包含目标接口实际需要的精确域名，不使用长期通配符。
- 脚本不得把请求、响应或标识符上传到外部服务。
- 日志只记录命中数量、分支结果和无敏感内容的错误类型，不输出完整 body 或 headers。
- 只修改实现目标所需字段；解析失败时保持原请求或响应。
- 不在源码中嵌入从抓包复制的真实账号响应、广告标识、签名或追踪参数。
- 规则、插件和脚本的 Raw 地址必须公开可读，但 Raw 内容本身不得依赖任何私有 Token。

## 提交前检查

对整个仓库和待提交差异执行大小写不敏感搜索，至少覆盖：

`token`、`access_token`、`refresh_token`、`session`、`sid`、`uuid`、`password`、`passwd`、`authorization`、`proxy-authorization`、`cookie`、`set-cookie`、`api-key`、`x-api-key`、`client-secret`、`private-key`、`ca-p12`、`ca-passphrase`、`pbk`、`ss://`、`vless://`、`hysteria2://`、`hy2://`。

还必须检查：

- URL 查询参数和片段中的高熵值；
- 长 Base64、JWT 和二维码；
- 新增的二进制文件、压缩包、HAR、日志、数据库、证书和配置备份；
- Git 暂存区及完整提交差异，而不只是当前工作目录；
- 文档、注释、测试样本和终端输出副本。

文档可以出现用于说明禁止项的字段名，但不得包含真实值、可用凭证或可恢复的编码版本。自动扫描只能作为辅助，不能替代人工检查。

## 发现泄露时

1. 立即停止推送、合并和发布，不在公开位置重复粘贴泄露值。
2. 对密码、Token、Cookie、订阅链接、证书或节点凭证先撤销或轮换；只删除文件不能使已经泄露的凭证失效。
3. 尚未提交时，从待提交内容中清除并重新扫描。
4. 已进入 Git 历史或 GitHub 时，评估并清理所有相关分支、标签、Pull Request、缓存、分叉和克隆；普通删除提交或 `git revert` 不会从历史中移除秘密。
5. 历史重写具有破坏性，必须单独制定计划、确认影响范围并协调现有克隆，不能作为普通修复直接执行。
6. 必要时联系 GitHub Support 清理缓存引用，并检查是否需要启用 Secret scanning 与 Push protection。

安全问题不要通过公开 Issue 附带原始材料报告。若需要协作，应先使用不含敏感值的描述建立联系，再约定私密传输方式。
